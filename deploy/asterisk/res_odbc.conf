; ODBC connection profile used by Sorcery realtime and func_odbc.
; Create matching DSN in /etc/odbc.ini (name: asterisk_cfg)

[asterisk_cfg]
enabled => yes
pre-connect => yes
dsn => asterisk_cfg
username => <DB_USER>
password => <DB_PASSWORD>
; Uncomment if needed:
; pooling => yes
; limit => 5
